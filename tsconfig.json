// tsconfig.json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    // 1. On définit la racine du projet
    "baseUrl": ".",
    // 2. On définit l'alias
    "paths": {
      "~/*": ["src/*"]
    }
  }
}